# micro-service-with-eureka-and-zuul

1. Run Discovery Server
2. Run Registry Service
3. Run Other Services

localhost:{Zuul-port}/{eureka-application-status-name}/{context-path-name}/{request-map-path}/{mapping-path}
